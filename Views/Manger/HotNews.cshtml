@model Maticsoft.Model.About
@{
    Layout = null;
    bool IsSuccess = (bool)ViewBag.IsSuccess;
}
@if (IsSuccess)
{
    <div class="alert alert-success with-icon" id="showMsg" style="transition: all 1s; ">
        <i class="icon-smile"></i>
        <div class="content">
            <h4>好消息</h4>
            修改成功！
        </div>
    </div>
    <script>
        let showMsg = document.getElementById('showMsg')
        console.log(showMsg)
        if (showMsg) {
            setTimeout(() => {
                showMsg.parentNode.removeChild(showMsg);
            }, 2000)
        }
    </script>
}
<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <meta name="renderer" content="webkit">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta name="viewport" content="width=device-width,initial-scale=1,minimum-scale=1,maximum-scale=1,user-scalable=no" />
    <title>自贡城乡管理执法信息系统</title>
    <link href="~/dist/lib/uploader/zui.uploader.min.css" rel="stylesheet" />
    <link href="~/dist/css/zui.min.css" rel="stylesheet">
    <link rel="stylesheet" type="text/css" href="~/static/admin/layui/css/layui.css" />
    <link rel="stylesheet" type="text/css" href="~/static/admin/css/admin.css" />
    <style>
        .layui-form-label {
            white-space: nowrap;
        }
    </style>
</head>

<body>
    <div class="page-content-wrap">
        <form class="layui-form" action="/Manger/HotNews/" method="post">
            <div class="layui-tab" style="margin: 0;">
                <div class="layui-tab-content">

                    <div class="layui-form-item">
                        <label class="layui-form-label">名称：</label>
                        <div class="layui-input-block">
                            <input type="text" name="PicURL" lay-verify="required" placeholder="请输入工作职位类型" autocomplete="off" class="layui-input" value="@Model.PicURL">
                        </div>
                    </div>
                    <div class="layui-form-item">
                        <label class="layui-form-label">时间：</label>
                        <div class="layui-input-block">
                            <input type="text" name="PicURL1" lay-verify="required" placeholder="请输入数量" autocomplete="off" class="layui-input" value="@Model.PicURL1">
                        </div>
                    </div>
                    <div class="layui-form-item">
                        <label class="layui-form-label">点赞：</label>
                        <div class="layui-input-block">
                            <input type="text" name="PicURL2" lay-verify="required" placeholder="请输入点赞数量" autocomplete="off" class="layui-input" value="@Model.PicURL2">
                        </div>
                    </div>
                    <div class="layui-tab-item layui-show">
                        <div class="layui-form-item">
                            <label for="PicURL" class="layui-form-label">图片:</label>
                            <input type="hidden" name="PicURL3" id="PicURL3" value="@Model.PicURL3" />
                            <div class="col-md-10">
                                <div id='PicUpload1' class="uploader" data-ride="uploader" data-url="/api/news/upload">
                                    <div class="uploader-message text-center">
                                        <div class="content"></div>
                                        <button type="button" class="close">×</button>
                                    </div>
                                    <div class="uploader-files file-list file-list-grid">
                                        <img style="width:80px;padding-left:10px;" src="@Model.PicURL3" />
                                    </div>
                                    <div>
                                        <hr class="divider">
                                        <div class="uploader-status pull-right text-muted"></div>
                                        <button type="button" class="btn btn-link uploader-btn-browse"><i class="icon icon-plus"></i> 选择文件</button>
                                        <button type="button" class="btn btn-link uploader-btn-start"><i class="icon icon-cloud-upload"></i> 开始上传</button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>


                    <div class="layui-form-item">
                        <label class="layui-form-label">名称：</label>
                        <div class="layui-input-block">
                            <input type="text" name="PicURL4" lay-verify="required" placeholder="请输入工作职位类型" autocomplete="off" class="layui-input" value="@Model.PicURL4">
                        </div>
                    </div>
                    <div class="layui-form-item">
                        <label class="layui-form-label">时间：</label>
                        <div class="layui-input-block">
                            <input type="text" name="PicURL5" lay-verify="required" placeholder="请输入数量" autocomplete="off" class="layui-input" value="@Model.PicURL5">
                        </div>
                    </div>
                    <div class="layui-form-item">
                        <label class="layui-form-label">点赞：</label>
                        <div class="layui-input-block">
                            <input type="text" name="PicURL6" lay-verify="required" placeholder="请输入点赞数量" autocomplete="off" class="layui-input" value="@Model.PicURL6">
                        </div>
                    </div>
                    <div class="layui-tab-item layui-show">
                        <div class="layui-form-item">
                            <label for="PicURL" class="layui-form-label">图片:</label>
                            <input type="hidden" name="PicURL7" id="PicURL7" value="@Model.PicURL7" />
                            <div class="col-md-10">
                                <div id='PicUpload2' class="uploader" data-ride="uploader" data-url="/api/news/upload">
                                    <div class="uploader-message text-center">
                                        <div class="content"></div>
                                        <button type="button" class="close">×</button>
                                    </div>
                                    <div class="uploader-files file-list file-list-grid">
                                        <img style="width:80px;padding-left:10px;" src="@Model.PicURL7" />
                                    </div>
                                    <div>
                                        <hr class="divider">
                                        <div class="uploader-status pull-right text-muted"></div>
                                        <button type="button" class="btn btn-link uploader-btn-browse"><i class="icon icon-plus"></i> 选择文件</button>
                                        <button type="button" class="btn btn-link uploader-btn-start"><i class="icon icon-cloud-upload"></i> 开始上传</button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="layui-form-item">
                        <label class="layui-form-label">名称：</label>
                        <div class="layui-input-block">
                            <input type="text" name="PicURL8" lay-verify="required" placeholder="请输入工作职位类型" autocomplete="off" class="layui-input" value="@Model.PicURL8">
                        </div>
                    </div>
                    <div class="layui-form-item">
                        <label class="layui-form-label">时间：</label>
                        <div class="layui-input-block">
                            <input type="text" name="PicURL9" lay-verify="required" placeholder="请输入数量" autocomplete="off" class="layui-input" value="@Model.PicURL9">
                        </div>
                    </div>
                    <div class="layui-form-item">
                        <label class="layui-form-label">点赞：</label>
                        <div class="layui-input-block">
                            <input type="text" name="PicURL10" lay-verify="required" placeholder="请输入点赞数量" autocomplete="off" class="layui-input" value="@Model.PicURL10">
                        </div>
                    </div>
                    <div class="layui-tab-item layui-show">
                        <div class="layui-form-item">
                            <label for="PicURL" class="layui-form-label">图片:</label>
                            <input type="hidden" name="PicURL11" id="PicURL11" value="@Model.PicURL11" />
                            <div class="col-md-10">
                                <div id='PicUpload3' class="uploader" data-ride="uploader" data-url="/api/news/upload">
                                    <div class="uploader-message text-center">
                                        <div class="content"></div>
                                        <button type="button" class="close">×</button>
                                    </div>
                                    <div class="uploader-files file-list file-list-grid">
                                        <img style="width:80px;padding-left:10px;" src="@Model.PicURL11" />
                                    </div>
                                    <div>
                                        <hr class="divider">
                                        <div class="uploader-status pull-right text-muted"></div>
                                        <button type="button" class="btn btn-link uploader-btn-browse"><i class="icon icon-plus"></i> 选择文件</button>
                                        <button type="button" class="btn btn-link uploader-btn-start"><i class="icon icon-cloud-upload"></i> 开始上传</button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    
                </div>
            </div>
            <div class="layui-form-item" style="padding-left: 10px;">
                <div class="layui-input-block">
                    <button class="layui-btn layui-btn-normal" lay-submit lay-filter="formDemo" type="submit">立即修改</button>
                </div>
            </div>
        </form>
    </div>

    <!-- jQuery (ZUI中的Javascript组件依赖于jQuery) -->
    <script src="~/dist/lib/jquery/jquery.js"></script>
    <script src="~/static/admin/layui/layui.js"></script>
    <script src="~/static/admin/js/common.js"></script>
    <script src="~/dist/lib/uploader/zui.uploader.min.js"></script>
    <script src="~/dist/lib/kindeditor/kindeditor.min.js"></script>

    <!-- 在此处编码你的创意 -->
    <!-- ZUI Javascript组件 -->
    <script src="~/dist/js/zui.min.js"></script>
    <script src="~/dist/lib/uploader/zui.uploader.min.js"></script>
    <link href="~/dist/lib/uploader/zui.uploader.min.css" rel="stylesheet">
    <link href="~/dist/lib/datetimepicker/datetimepicker.min.css" rel="stylesheet">
    <script src="~/dist/lib/datetimepicker/datetimepicker.min.js"></script>
    <link rel="stylesheet" type="text/css" href="~/static/admin/layui/css/layui.css" />
    <link rel="stylesheet" type="text/css" href="~/static/admin/css/admin.css" />
    <script type="text/javascript">
        $('#PicUpload1').uploader({
            onFileUploaded: function (file, res) {
                if (file) {
                    var ret = $.parseJSON(res.response);
                    $('#PicURL3').val('/uploadshop/' + ret.data);
                };
            }
        });
        $('#PicUpload2').uploader({
            onFileUploaded: function (file, res) {
                if (file) {
                    var ret = $.parseJSON(res.response);
                    $('#PicURL7').val('/uploadshop/' + ret.data);
                };
            }
        });
        $('#PicUpload3').uploader({
            onFileUploaded: function (file, res) {
                if (file) {
                    var ret = $.parseJSON(res.response);
                    $('#PicURL11').val('/uploadshop/' + ret.data);
                };
            }
        });
    </script>
    <script>
        layui.use(['form', 'jquery', 'laydate', 'layer', 'laypage', 'dialog', 'element', 'upload', 'layedit'], function () {
            var form = layui.form(),
                layer = layui.layer,
                $ = layui.jquery,
                laypage = layui.laypage,
                laydate = layui.laydate,
                layedit = layui.layedit,
                element = layui.element(),
                dialog = layui.dialog;

            //获取当前iframe的name值
            var iframeObj = $(window.frameElement).attr('name');

        });

    </script>
</body>

</html>